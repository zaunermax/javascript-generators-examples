# Editor configuration, see http://editorconfig.org
root = true

# tab size intentionally left out, everybody should set that individually

[*]
charset = utf-8
indent_style = tab
insert_final_newline = true
trim_trailing_whitespace = true
end_of_line = lf
max_line_length = 80

[*.md]
trim_trailing_whitespace = false